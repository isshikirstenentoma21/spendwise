import 'package:flutter_test/flutter_test.dart';

import 'package:spendwise/main.dart';

void main() {
  test('SpendWise app can be constructed', () {
    expect(const SpendWiseApp(), isA<SpendWiseApp>());
  });
}
